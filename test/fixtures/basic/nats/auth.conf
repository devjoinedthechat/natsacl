# Generated by natsacl 0.1.0. Do not edit — run `natsacl compile`; CI runs `natsacl check`.
# Users: ingest, alerts-svc, admin (unrestricted)

authorization {
  users: [
    {
      user: "ingest"
      password: $INGEST_NATS_PASSWORD
      permissions: {
        publish: {
          allow: [
            "$JS.ACK.TELEMETRY.ingest-sensor-readings.>"
            "$JS.API.CONSUMER.CREATE.TELEMETRY.ingest-sensor-readings.SENSORS.reading"
            "$JS.API.CONSUMER.INFO.TELEMETRY.ingest-sensor-readings"
            "$JS.API.CONSUMER.MSG.NEXT.TELEMETRY.ingest-sensor-readings"
            "$JS.API.INFO"
            "$JS.API.STREAM.INFO.TELEMETRY"
            "ALERTS.raised"
            "CATALOG.LOOKUP"
            "OPTICAL.TASKS"
            "SENSORS.*.status"
            "SENSORS.*.trimmed"
            "SENSORS.calibrated.*"
            "SENSORS.firmware"
            "SENSORS.heartbeat.*"
            "SENSORS.legacy.>"
            "SENSORS.notified"
            "SENSORS.priority"
            "SENSORS.reading"
            "SENSORS.routine"
            "THERMAL.TASKS"
          ]
        }
        subscribe: {
          allow: [
            "_INBOX.>"
          ]
        }
      }
    }
    {
      user: "alerts-svc"
      password: $ALERTS_NATS_PASSWORD
      permissions: {
        publish: {
          allow: [
            "$JS.ACK.TELEMETRY.*.>"
            "$JS.API.CONSUMER.CREATE.TELEMETRY.*.ALERTS.escalated"
            "$JS.API.CONSUMER.CREATE.TELEMETRY.alerts-acks.ALERTS.acknowledged"
            "$JS.API.CONSUMER.CREATE.TELEMETRY.alerts-incidents.INCIDENTS.>"
            "$JS.API.CONSUMER.CREATE.TELEMETRY.alerts-thresholds.ALERTS.cleared"
            "$JS.API.CONSUMER.CREATE.TELEMETRY.alerts-thresholds.ALERTS.raised"
            "$JS.API.CONSUMER.INFO.TELEMETRY.*"
            "$JS.API.CONSUMER.MSG.NEXT.TELEMETRY.*"
            "$JS.API.INFO"
            "$JS.API.STREAM.INFO.TELEMETRY"
            "$JS.API.STREAM.LIST"
            "$JS.API.STREAM.NAMES"
            "INCIDENTS.closed"
            "INCIDENTS.opened"
          ]
        }
        subscribe: {
          allow: [
            "_INBOX.>"
            "CATALOG.LOOKUP"
            "alerts.TASKS"
          ]
        }
        allow_responses: true
      }
    }
    {
      user: "admin"
      password: $ADMIN_NATS_PASSWORD
      # no permissions block: unrestricted, for provisioning streams and operations
    }
  ]
}
